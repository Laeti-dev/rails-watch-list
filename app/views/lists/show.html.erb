<div class="container-fluid">
<%# BANNER %>
  <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://images.unsplash.com/photo-1598899134739-24c46f58b8c0?ixlib=rb-1.2.1&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1456&q=80)">
    <div class="d-flex justify-content-end banner-buttons">
      <%= link_to 'Add to the list', new_list_bookmark_path(@list), class: 'btn btn-primary' %>
      <%= link_to 'Back', root_path, class: 'btn btn-primary mx-2' %>
    </div>
    <div class="container d-flex justify-content-center">
      <h1><%= @list.name %></h1>
    </div>
  </div>
<%# LIST OF BOOKMARKED %>

  <div class="row">
    <% @list.bookmarks.each do |bookmark| %>
      <div class="card col-3 mx-2 my-2" style="width: 18rem;">
        <% if bookmark.photo.attached? %>
          <%= cl_image_tag bookmark.photo.key, width: 300, height: 300, crop: :fill %>
        <% else %>
          <%= image_tag ("https://image.tmdb.org/t/p/w500/#{bookmark.movie.poster_url}") %>
        <% end %>
        <div class="card-body">
          <h5 class="card-title"><%= bookmark.movie.title %></h5>
          <h6 class="card-subtitle mb-2 text-muted"><%= bookmark.movie.rating %></h6>
          <p class="card-text"><%= bookmark.movie.overview %></p>
          <%= link_to 'Delete movie', bookmark_path(bookmark), method: :delete, data: { confirm: "Are you sure?" } %>
            <%# <i class="fas fa-trash-alt"></i>
          <% end %>
        </div>
      </div>
      <%# <li><%= Bookmark.find_by(list: @list, movie: movie).comment </li> %>
    <% end %>
  </div>
</div>
